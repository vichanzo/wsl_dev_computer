---
 - name: Install npm
   apt:
     name: ['npm']
     state: present
     update_cache: yes
 - name: Install "create-react-app" node.js package globally.
   community.general.npm:
     name: create-react-app
     global: true
